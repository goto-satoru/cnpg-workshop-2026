---
apiVersion: v1
kind: Namespace
metadata:
  name: edb
---
apiVersion: postgresql.k8s.enterprisedb.io/v1
kind: Cluster
metadata:
  name: epas16
  namespace: edb
  annotations:
    alpha.k8s.enterprisedb.io/unrecoverable: "true"
spec:
  instances: 3
  imageName: docker.enterprisedb.com/k8s/edb-postgres-advanced:16.13
  imagePullSecrets:
  - name: edb-pull-secret
  storage:
    size: 1Gi
  postgresql:
    parameters:
      # to mimic 'replication slot cannot be advanced' Error 
      max_slot_wal_keep_size: "64MB"
      wal_keep_size: "0"
      # to frequently trigger checkpoints and promote WAL recycling
      min_wal_size: "32MB"
      max_wal_size: "64MB"
      checkpoint_timeout: "30s"
